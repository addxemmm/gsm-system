; All GSM SIP services share one container. Only OpenBTS loopback contacts are valid.
; GSM SIP 服务同容器运行，仅允许 OpenBTS 的本机联系人，不开放外部注册目标。
contactdeny=0.0.0.0/0.0.0.0
contactdeny=::/0
contactpermit=127.0.0.1/255.255.255.255
